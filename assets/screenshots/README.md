# Screenshots

Drop UI screenshots here, e.g.:

- `dashboard-ok.png` — dashboard in OK state
- `dashboard-alarm.png` — dashboard in ALARM state after a spike
- `architecture.png` — high-level architecture diagram

Then reference them in the main `README.md` with:

```markdown
![Dashboard OK](../assets/screenshots/dashboard-ok.png)
```
